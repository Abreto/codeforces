/* Codeforces Round #628 (Div. 2). Problem A. */
#include <bits/stdc++.h>

using namespace std;

namespace abreto {

void main(void)
{
    int t, x;
    cin >> t;
    while (t--) {
        cin >> x;
        cout << 1 << ' ' << (x - 1) << '\n';
    }
}

}

int main(void)
{
    cin.sync_with_stdio(false); cin.tie(0);
    cout << fixed << setprecision(12);
    abreto::main();
    return 0;
}
